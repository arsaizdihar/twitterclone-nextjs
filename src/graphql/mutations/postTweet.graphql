mutation PostTweet($text: String!, $file: Upload) {
  postTweet(text: $text, file: $file) {
    success
    tweet {
      id
      pk
      text
      createdAt
      likesCount
      retweetCount
      isLiked
    }
  }
}
